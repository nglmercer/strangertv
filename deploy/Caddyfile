# Place behind DNS A/AAAA for chat.example.com
# caddy run --config deploy/Caddyfile

chat.example.com {
	encode gzip
	reverse_proxy 127.0.0.1:8787
	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
	}
}

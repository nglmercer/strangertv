# ts-rs writes the generated TypeScript straight into the client's source tree,
# so `cargo test` is the only step needed to refresh the wire contract.
[env]
TS_RS_EXPORT_DIR = { value = "../shared/generated", relative = true }
